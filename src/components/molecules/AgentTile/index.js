import React from 'react';

import agentPropType from 'sly/propTypes/agent';


const AgentTile = ({
  agent,
}) => (
  <div>
  </div>
);

AgentTile.propTypes = {
  agent: agentPropType,
};


export default AgentTile;
