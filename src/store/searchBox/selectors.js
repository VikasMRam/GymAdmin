export const initialState = {
  address: '',
  location: null,
};

export const searchBoxAddress = (state = initialState) => state.address;
export const searchBoxLocation = (state = initialState) => state.location;
